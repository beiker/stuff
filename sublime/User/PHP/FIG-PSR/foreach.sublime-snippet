<snippet>
    <content><![CDATA[
foreach (\$${1:iterable} as \$${2:key} ${3:=> \$${4:value}})
{
  ${5:// foreach body}
}
]]></content>
    <tabTrigger>foreach</tabTrigger>
    <scope>source.php</scope>
    <description>PSR foreach</description>
</snippet>
